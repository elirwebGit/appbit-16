# ─── PWA / Vite ────────────────────────────────────────────────────────────────
# Exemplo de arquivo .env para o PWA. Copie para .env (dev) ou .env.production (prod).
# NUNCA comite arquivos .env com valores reais.

# URL da API (Railway em produção, localhost em dev)
VITE_API_URL=http://localhost:3000/api
